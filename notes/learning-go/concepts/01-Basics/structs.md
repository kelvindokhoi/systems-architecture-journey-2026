# Structs in Go

### 1. Struct in Go
Struct in Go can be represented like this:
```go
type car struct {
	brand      string
	model      string
	doors      int
	mileage    int
}
```

Structs in Go = Class in Python.